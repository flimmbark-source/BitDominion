{
  "name": "bitdominion-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port ${PORT:-5173}",
    "build": "tsc && vite build",
    "postbuild": "node ./scripts/sync-static-assets.mjs",
    "preview": "vite preview --host 0.0.0.0 --port ${PORT:-4173}"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.6.2",
    "vite": "^5.4.8"
  }
}
